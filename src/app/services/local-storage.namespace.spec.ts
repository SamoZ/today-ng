import { LocalStorage.Namespace } from './local-storage.namespace';

describe('LocalStorage.Namespace', () => {
  it('should create an instance', () => {
    expect(new LocalStorage.Namespace()).toBeTruthy();
  });
});
